
look up config.jtAuthenticationFilter
private final JwtService jwtService;
    private  UserService userService;




    If the method is not being called in the context of an HTTP request, you will likely need to handle the AccessDeniedException yourself
    @PreAuthorize("hasAuthority('permission:read') || hasRole('ADMIN')")


purchaseRepo.findById(id).ifPresent(purchase -> {});

 <T> List<T> findAll(Class<T> type); //Cannot be overridden

 do we need to write exception for every request to repo

 ie UserServiceImpl
 if a service class impelemwnt a spring secuirty interface,
 override the menthod in the spring sec interface
 when later called spring  secuirty interface, it use the overridden methoid